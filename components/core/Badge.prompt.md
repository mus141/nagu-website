Small count / status indicator with the hard black keyline. Round for numeric counts (cart, notifications), block for short status flags.

```jsx
<Badge tone="red">3</Badge>
<Badge tone="ink" shape="block">SOLD OUT</Badge>
```

Tones: `red | ink | gold | cream`. Shapes: `round | block`.
